<Window x:Class="InheritanceDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InheritanceDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="tbSuperOut"  FontSize="20" ></TextBlock>
        <TextBlock x:Name="tbPersonOut" FontSize="20"  Grid.Row="1" ></TextBlock>
        <Button x:Name="btnDoSomething" Content="Click Me" Grid.Row="2" Click="btnDoSomething_Click" />
        <Button x:Name="btnCopyStuff" Content="Copy Things" Grid.Row="2" Grid.Column="1" Click="btnCopyStuff_Click" />
    </Grid>
</Window>
